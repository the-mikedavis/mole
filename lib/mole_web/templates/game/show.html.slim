h1 Nice work!

= if @sets_left do
  p = "You have #{@sets_left - 1} sets left. Keep playing or take a break."

= link("Keep playing", to: Routes.game_path(@conn, :index))
br
= link("Home", to: Routes.page_path(@conn, :index))
